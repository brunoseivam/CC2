{ leitura de nome e idade com escrita de mensagem usando estes dados }



algoritmo
	declare
		nome, genero, classe, ordem: literal
	declare
		idade: inteiro
	declare
		idade: literal

	{ leitura de nome e idade do teclado }
	escreva("Nome: ")
	leia(nome)
	escreva("Idade: ")
	leia(idade)

	{ saída da mensagem na tela }
	escreva(nome, " tem ", idade, " anos.")

fim_algoritmo
